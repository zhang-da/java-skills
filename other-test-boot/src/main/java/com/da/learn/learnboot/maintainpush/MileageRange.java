package com.da.learn.learnboot.maintainpush;

public class MileageRange extends Range<Double> {

    public MileageRange(Integer stage, Integer section, Double start, Double end) {
        super(stage, section, start, end);
    }
}
